query MovieQuery($id: ID!) {
    movie(id: $id) {
        id
        storyline
        likes
        dislikes
        actors {
            name
            id
        }
        genres {
            name
            id
        }
    }
}